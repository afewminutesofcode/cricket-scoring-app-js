import React from "react";
import { render } from "react-dom";

import "./styles.css";
const App = () => (
  <div className="app-wrap">
    <h1>Cricket Scoing App</h1>
  </div>
);

render(<App />, document.getElementById("root"));
